<div class="numberSelectCon">
	<button class="numBtn" disabled="{{ minusDisabled }}" grab:touchstart.bubble="handleMinus">-</button>

<!-- todo input无法居中，更新数据文本会消失，暂时先用text渲染 -->
<!--<input class="input" type="number" maxlength="3" enterkeytype="done" caret-color="#eee">{{ numbers }}-->
<!--</input>-->
	<text class="input">{{ numbers }}</text>
	<button class="numBtn" disabled="{{ plusDisabled }}" grab:touchstart.bubble="handlePlus">+</button>
</div>
